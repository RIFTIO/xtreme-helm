apiVersion: v1
kind: Service
metadata:
  name: {{ include "file-mgr.fullname" . }}
  labels:
    {{- include "file-mgr.labels" . | nindent 4 }}
  namespace: {{ include "sdnc.namespace" . }}
spec:
  {{- if .Values.service.useNodePort }}
  type: NodePort
  {{- end }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      {{- if .Values.service.useNodePort }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
      protocol: TCP
      name: http
  selector:
    {{- include "file-mgr.selectorLabels" . | nindent 4 }}
