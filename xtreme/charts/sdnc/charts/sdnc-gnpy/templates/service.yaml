apiVersion: v1
kind: Service
metadata:
  name: {{ include "gnpy.fullname" . }}
  namespace: {{ include "sdnc.namespace" . }}
  labels:
    {{- include "gnpy.labels" . | nindent 4 }}
spec:
  {{- if .Values.service.useNodePort }}
  type: NodePort
  {{- end }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      {{- if .Values.service.useNodePort }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
      protocol: TCP
      name: http
  selector:
    {{- include "gnpy.selectorLabels" . | nindent 4 }}