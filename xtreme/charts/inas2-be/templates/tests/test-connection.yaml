apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inas2-be.fullname" . }}-test-connection"
  labels:
    {{- include "inas2-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: {{ .Values.global.busybox.image.repository }}:{{ .Values.global.busybox.image.tag }}
      command: ['wget']
      args: ['{{ include "inas2-be.fullname" . }}:{{ .Values.service.port12 }}']
  restartPolicy: Never
