# values for setting the hostname. These are needed for k3s and other some other
# installations

# the hostname should match the riftExternalAddress

global: 
  hostAliases:
  - ip: "10.157.136.200"
    hostnames:
    - "jlm1.lab.dzsi.local"
  ingress:
    riftExternalAddress: jlm.lab.dzsi.local
